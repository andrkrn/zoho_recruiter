require 'spec_helper'

describe ZohoRecruiter do
  it 'has a version number' do
    expect(ZohoRecruiter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
